# JobScraper

Project scaffold for a TypeScript Node.js backend and React TypeScript frontend.

The structure follows `constitution.md`:

- backend scraping, parsing, services, API, and utilities are separated
- frontend components, pages, hooks, services, and types are separated
- shared data contracts live in `packages/shared`

